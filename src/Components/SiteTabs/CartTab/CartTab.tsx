import React from 'react';
import {IPropsCart} from "../../../Interfaces/Interfaces";

const CartTab = ({cart,setCart}:IPropsCart) => {
    return (
        <div>
            cart
        </div>
    );
};

export default CartTab;